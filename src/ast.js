/**
 * @file
 * This is a dummy export file.
 *
 * It exists so that ./nodes.js can import type definitions from ./ast.ts using ESM import syntax
 * (instead of the verbose JSDoc import syntax) without throwing a runtime error.
 */

export {};
